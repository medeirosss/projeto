# Frontend - Etapa 3E

Adicionar botão separado:

```text
Executar LAB
```

Mostrar apenas se:

```text
approved_for_execution = true
approved_for_lab = true
risk_level = low
executor_name = powershell
requires_reboot = false
```

Não reaproveitar o botão de dry-run.